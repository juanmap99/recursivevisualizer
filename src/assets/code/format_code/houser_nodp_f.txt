$sdef$s $dmaxProfitRobberHelper$d(nums,index):
    $sif$s index < $u0$u: $sreturn$s $u0$u
    robar = nums[index] + maxProfitRobberHelper(nums,index-$u2$u)
    no_robar = maxProfitRobberHelper(nums,index-$u1$u)
    maxProfit = max(robar,no_robar)
    $sreturn$s maxProfit

$d$d

$sdef$s $dmaxProfitRobber$d(nums):
    index = $flen$f(nums)-$u1$u
    $sreturn$s maxProfitRobberHelper(nums,index)
    