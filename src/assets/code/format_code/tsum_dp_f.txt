$sdef$s $dtotalTargetSumHelper$d(nums,target,index,curr_sum,memo):
    $sif$s (index,curr_sum) $sin$s memo:
        $sreturn$s memo[(index,curr_sum)]
    $sif$s index < $u0$u $sand$s curr_sum == target:
        $sreturn$s $u1$u
    $sif$s index < $u0$u:
        $sreturn$s $u0$u
    
    solsSumando = totalTargetSumHelper(nums,target,index-$u1$u,
                                    curr_sum + nums[index],memo)
    solsRestando = totalTargetSumHelper(nums,target,index-$u1$u,
                                    curr_sum + -nums[index],memo)
    
    totalSol = solsSumando + solsRestando
    memo[(index,curr_sum)] = totalSol

    $sreturn$s memo[(index,curr_sum)]

$d$d

$sdef$s $dtotalTargetSum$d(nums,target):
    memo = {}
    index = $flen$f(nums)-$u1$u
    $sreturn$s totalTargetSumHelper(nums,target,$flen$f(nums)-$u1$u,$u0$u,memo)